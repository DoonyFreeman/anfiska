# Анфиса для друзей

## Установка окружения

### Склонируйте проект «Анфиса для друзей» в папку _Dev/_.

Должна получиться такая структура:

```
Dev/
 └── anfisa1sprint/
     ├── anfisa_for_friends/
     ├── html_templates/
     ├── .gitignore
     ├── LICENSE
     ├── requirements.txt
     └── README.md
```

### Создайте виртуальное окружение

1. Запустите редактор Visual Studio Code и через меню «_Файл» / «Открыть директорию»_ откройте папку _Dev/anfisa1sprint/_.
2. Запустите терминал в VS Code, удостоверьтесь, что вы работаете из директории _anfisa1sprint/_ (если вы работаете под Windows, убедитесь, что в терминале запущен Git Bash, а не PowerShell или что-нибудь ещё), и выполните команду:

- Linux/macOS
  ```bash
  python3 -m venv venv
  ```
- Windows
  ```python
  python -m venv venv
  ```

В директории _anfisa1sprint/_ будет развёрнуто виртуальное окружение и появится папка `venv`, в которой будут храниться все зависимости проекта, а структура файлов станет такой:

```

Dev/
└── anfisa1sprint/
    ├── anfisa_for_friends/
    ├── html_templates/
    ├── venv/
    ├── .gitignore
    ├── LICENSE
    ├── requirements.txt
    └── README.md
```

### Активация виртуального окружения

в терминале перейдите в корневую директорию проекта _Dev/anfisa1sprint/_ и выполните команду:

- Linux/macOS
  ```bash
  source venv/bin/activate
  ```
- Windows
  ```bash
  source venv/Scripts/activate
  ```

Теперь все команды в терминале будут предваряться строкой `(venv)`.

💡 Все дальнейшие команды в терминале надо выполнять с активированным виртуальным окружением.

Обновите pip:

```bash
python -m pip install --upgrade pip
```

### Установка зависимостей из файла _requirements.txt_:

Находясь в папке _Dev/anfisa1sprint/_, выполните команду:

```bash
pip install -r requirements.txt
```

### Применение миграций

В директории с файлом manage.py выполните команду:

```bash
python manage.py migrate
```

#### Создание записей меню мороженого

Новое приложение `ice_cream` хранит ассортимент, стоимость и
наличие товара. После выполнения миграций вы можете управлять
записями через встроенную админку.

1. Создайте суперпользователя (если ещё не создан):

   ```bash
   python manage.py createsuperuser
   ```

2. Запустите сервер и откройте [http://127.0.0.1:8000/admin/](http://127.0.0.1:8000/admin/).
3. В разделе «Ice cream» добавьте новые экземпляры, указав название, описание, цену и флаг доступности.

Поля `price` (Десятичное) и `is_available` (наличие) будут отображаться
в карточках каталога автоматически.

> Если в базе данных нет ни одной записи, на сайте показываются демонстрационные
> позиции вместе с заметным сообщением. Ссылки на них не ведут в админку, но
> работать страница товара будет (т.е. 404 больше не возникнет). Как только вы
> добавите свои позиции, демонстрационные данные больше не будут показываться.

### Запуск проекта в dev-режиме

В директории с файлом manage.py выполните команду:

```bash
python manage.py runserver
```

В ответ Django сообщит, что сервер запущен и проект доступен по адресу [http://127.0.0.1:8000/](http://127.0.0.1:8000/).
